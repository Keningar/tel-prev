/*
 * Actualización del parámetro de tareas de alquiler de servidor de soluciones DC.
 */
UPDATE DB_GENERAL.ADMI_PARAMETRO_DET
  SET VALOR3 = 'REVISAR ESTADO SERVIDORES DE STORAGE',
      VALOR6 = 'TAREAS DE TI - STORAGE'
WHERE PARAMETRO_ID = (
    SELECT ID_PARAMETRO FROM DB_GENERAL.ADMI_PARAMETRO_CAB WHERE NOMBRE_PARAMETRO = 'HOSTING TAREAS POR DEPARTAMENTO'
  )
  AND DESCRIPCION = 'FACTIBILIDAD POOL RECURSOS - ALQ';
COMMIT;
/
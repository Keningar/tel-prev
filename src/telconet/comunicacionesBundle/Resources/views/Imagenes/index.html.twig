{% extends "::layout_completo.html.twig" %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" type="text/css" media="screen" href="{{ asset('./bundles/comunicaciones/css/Imagenes.css') }}" />
{% endblock %}

{% block javascripts%}
    {{ parent() }}
    <script>
        var strUrlModelosElementos = '{{ path('search_ajaxGetModelosElementos') }}';
        var strUrlGetDepartamentos = '{{ path('infocaso_getDepartamentosPorEmpresaYCiudad') }}';
        var strUrlDescargarImagen  = '{{ path('comunicacionesimagenes_descargarImagenes') }}';
        var strUrlTiposElementos   = '{{ path('search_ajaxGetTiposElementos') }}';
        var strUrlGetImagenes      = '{{ path('comunicacionesimagenes_ajaxBuscarImagenes') }}';
        var strUrlGetEmpresas      = '{{ path('infocaso_ajaxGetEmpresasPorSistema') }}';
        var strUrlElementos        = '{{ path('search_ajaxGetElementos') }}';
        var strUrlChooser          = '{{ asset('./public/js/ExtJsChooser') }}';
        var strUrlUx               = '{{ asset('./public/js/ext-4.1.1/src/ux') }}';
        var strUrlGuardarEvaluacion= '{{ path('comunicacionesimagenes_guardarEvaluacionImagenes') }}';
        var strUrlIniciarEvaluacion= '{{ path('comunicacionesimagenes_iniciarEvaluacionImagenes') }}';
    </script>   
    <script type="text/javascript" src="{{ asset('bundles/comunicaciones/js/Imagenes/index.js?5') }}"></script>  
    <script type="text/javascript" src="https://maps.google.com/maps/api/js?sensor=false"></script>               
{% endblock %}


{% block contenido_central %} 
    <div id="titulo_content">
        <img src="{{ asset('public/images/ico_gallery.png' | default('tools1.png') ~ '') }}"
             class="imagen-titulo-content" >
        <h1>Im&aacute;genes</h1>
    </div>
    <div class="dashboard_content k-content">
                    <div id = "dr">
                    </div>
        <div id="example" class="k-content">
            {% if(is_granted('ROLE_292-1')) %}
                <div id='filtroFacturas'>
                    <div id = "fieldsTabComercial">
                    </div>
                    <div id = "fieldsTabTecnico">
                    </div>
                    <div id = "fieldsTabSoporte">
                    </div>
                </div>
                <div id='visorImagenes' style="clear:both; margin-top: 20px;"></div>
            {% else %}
                <div class="info-error"> No tiene permisos para hacer uso de la opción </div>
                <div class="buttons">
                    <p>
                        <a class="cancel-crud" href="{{ path('modulo',{'modulo':'comunicaciones'}) }}">
                            Regresar
                        </a>
                    </p>
                </div>
            {% endif %}
        </div>
    </div>
{% endblock contenido_central %}

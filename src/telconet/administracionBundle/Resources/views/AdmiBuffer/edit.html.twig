{% extends "::layout.html.twig" %}

{% block stylesheets %}
	{{ parent() }}
{% endblock %}

{% block javascripts%}
	{{ parent() }}
    <script type="text/javascript" src="{{ asset('./bundles/administracion/js/AdmiBuffer/edit.js?1') }}"></script>
    <script>
        var getHilos                    = '{{ path('admihilo_getHilosPorEstado') }}';
        var getBuffersHilos             = '{{ path('admibuffer_getBuffersHilos', {'id':buffer.id}) }}';
        var getClaseTipoMedioPorBuffer  = '{{ path('admibuffer_getClaseTipoMedioPorBuffer', {'id':buffer.id}) }}';
    </script>
{% endblock %}

{% block contenido_izq %}
    <div id="titulo_content">
        <img src="{{ asset('public/images/'~ item.urlImagen | default('tools1.png') ~ '') }}" class="imagen-titulo-content">
        <h1>Editar Buffer</h1>
    </div>
    {% for flashMessage in app.session.flashbag.get('notice') %}
        <div class="errormessage">
            {{ flashMessage }}
        </div>
    {% endfor %}
    <div class="body_content">
        <form id='form_edit_buffer' action="{{ path('admibuffer_update', { 'id': buffer.id }) }}" method="post" {{ form_enctype(edit_form) }} 
              class="formulario" novalidate="novalidate" onsubmit="return validar();">
            {{ form_widget(edit_form) }}
            <div id="grid"></div>
            <input type="hidden" name="bufferHilo" id="bufferHilo" value="" />
            <div class="buttons">
                <p>
                    <input type='submit'class="button-crud" value="Guardar" />
                    <span class="height20px">
                        &oacute;&nbsp;
                        <a class="button-crud" href="{{ path('admibuffer') }}">
                            Cancelar
                        </a>
                    </span>
                </p>
            </div>
        </form>
    </div>
{% endblock contenido_izq %}

{% block javascripts_footer%}
<script>
{% endblock javascripts_footer%}

{% block contenido_der %}
    <ul class="form_actions">
        <li>
            
        </li>
    </ul>
    <div class="titulo-acciones-relacionadas">
            Acciones Relacionadas
    </div>

{% endblock contenido_der %}


DELETE FROM DB_COMERCIAL.ADMI_CARACTERISTICA x
WHERE x.DESCRIPCION_CARACTERISTICA = 'ID_CABECERA_PLANIF';

DELETE FROM DB_GENERAL.ADMI_PARAMETRO_DET x
	WHERE x.VALOR1 = 'URL_BASE_HALL';


UPDATE DB_SOPORTE.ADMI_TIPO_EVENTO x
SET x.CODIGO='ALMUERZO', x.DESCRIPCION='EVENTO QUE SE GENERA PARA NOTIFICAR ALIMENTACION DEL PERSONAL', x.NOMBRE = 'Almuerzo'
WHERE x.ID_TIPO_EVENTO = 2;
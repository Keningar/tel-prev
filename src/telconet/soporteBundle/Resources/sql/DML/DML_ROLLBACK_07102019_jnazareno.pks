--Reverso del insert del progreso de regularización
DELETE FROM DB_SOPORTE.INFO_PROGRESO_PORCENTAJE WHERE TIPO_PROGRESO_ID =
(
        select ID_TIPO_PROGRESO 
        from DB_SOPORTE.ADMI_TIPO_PROGRESO 
        where CODIGO = 'REGULARIZACION'
);

DELETE FROM DB_SOPORTE.ADMI_TIPO_PROGRESO WHERE CODIGO = 'REGULARIZACION';

COMMIT;

 /
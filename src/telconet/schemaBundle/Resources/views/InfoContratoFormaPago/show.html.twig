<h1>InfoContratoFormaPago</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Mesvencimiento</th>
            <td>{{ entity.mesVencimiento }}</td>
        </tr>
        <tr>
            <th>Aniovencimiento</th>
            <td>{{ entity.anioVencimiento }}</td>
        </tr>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Numeroctatarjeta</th>
            <td>{{ entity.numeroCtaTarjeta }}</td>
        </tr>
        <tr>
            <th>Numerodebitobanco</th>
            <td>{{ entity.numeroDebitoBanco }}</td>
        </tr>
        <tr>
            <th>Codigoverificacion</th>
            <td>{{ entity.codigoVerificacion }}</td>
        </tr>
        <tr>
            <th>Titularcuenta</th>
            <td>{{ entity.titularCuenta }}</td>
        </tr>
        <tr>
            <th>Fecreacion</th>
            <td>{{ entity.feCreacion|date('Y-m-d H:i:s') }}</td>
        </tr>
        <tr>
            <th>Feultmod</th>
            <td>{{ entity.feUltMod|date('Y-m-d H:i:s') }}</td>
        </tr>
        <tr>
            <th>Usrcreacion</th>
            <td>{{ entity.usrCreacion }}</td>
        </tr>
        <tr>
            <th>Usrultmod</th>
            <td>{{ entity.usrUltMod }}</td>
        </tr>
        <tr>
            <th>Estado</th>
            <td>{{ entity.estado }}</td>
        </tr>
        <tr>
            <th>Tipocuenta</th>
            <td>{{ entity.tipoCuenta }}</td>
        </tr>
        <tr>
            <th>Ipcreacion</th>
            <td>{{ entity.ipCreacion }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('infocontratoformapago') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('infocontratoformapago_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('infocontratoformapago_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
